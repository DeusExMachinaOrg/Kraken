# §140. Mirotvorec01: план приведения руления под газом к поведению ODE

Продолжение §139.8-139.10 (задачи #62, #63). Там был поставлен диагноз «дребезг привода» и
опровергнуты две дешёвые гипотезы (бюджет солвера, демпфирование мотора). Этот документ —
план доведения до рабочего состояния, написанный ПОСЛЕ дополнительного сбора фактов, а не
поверх предположений §139.9.

## §140.1. Что выяснилось при подготовке плана (и одна опровергнутая собственная ошибка)

Перед планированием перечитал уже снятый лог (`diag_mirotvorec_stuck.log`, 77 тыс. строк) и код,
и нашёл вещи, которые меняют картину. Всё ниже — измерено/прочитано, не предположено.

**(1) ОШИБКА в §139.8, исправляю.** Вывод «`jolt_travel_m` побитово одинаков → детерминированный
отказ» неверен. `0.7615773105865042` = `sqrt(0.3² + 0.7²)` (сходится до 12 значащих цифр). Строка
`Shadow divergence` печатает COM форматом `%.1f` — **один знак после запятой**. Значит все
`ode_travel_m`/`jolt_travel_m`/`ratio` во ВСЁМ §139 (включая обе 23-машинные таблицы) считаются по
координатам, квантованным шагом 0.1 м. Для машин, проезжающих 15-30 м, это ~0.3% и не мешает; для
«застрявшей» тени, проезжающей <1 м, разрешение измерения сравнимо с самим измеряемым. Значения
`0.100` и `0.000` у бюджетов 60/30 и 100/50 в §139.10 — это «один шаг квантования» и «меньше
одного шага», а не «ровно 10 см» и «ровно ноль». Тренд (больше итераций → меньше движения)
остаётся, его величина — нет.

**(2) Машина НЕ сломана всегда — застревает только при старте «на холодную».** В 23-машинном
прогоне §139.7 `Mirotvorec01` под тем же рулевым сценарием проехал **17.6 м** (ratio 1.16, вполне
нормально). В моих же повторах §139.8/§139.10 — <1 м, все 12 из 12. Разница в методике: в флотском
прогоне в ОДНОЙ сессии сначала отрабатывает `accel_brake` (машина уезжает по прямой ~155 м) и
только потом `accel_steer_brake`; мои повторы гоняли рулевой сценарий сразу после загрузки сейва.
То есть отказ зависит от начального состояния тени, а не от машины как таковой. Это же объясняет,
почему §139.7 не заметил проблему, а прицельная перепроверка §139.8 её увидела.

**(3) Топология: два рулевых моста на разных продольных позициях.** Из `vehicles.xml:1239-1246` +
живого `docs §28` лога (`foreAft`): колёса 0,1 — передний мост (`foreAft=+3.600`, рулевой), колёса
4,5 — **средний** мост (`foreAft=0.000`, тоже рулевой), колёса 2,3 — задний (`foreAft=-3.600`, не
рулевой). Два рулевых моста на РАЗНОМ продольном удалении, получающие ОДИН И ТОТ ЖЕ угол, требуют
разных центров поворота — кинематически несовместимо, шины обязаны скрести. НЕ уникально для
Mirotvorec: та же топология у `Cruiser` (8 колёс/4 рулевых) и `Tank` (6/4). Оба в базовом прогоне
ехали нормально (Cruiser01 ratio 0.70, Tank01 1.82) — значит одна топология сама по себе отказ не
объясняет, она лишь повышает чувствительность.

**(4) Ход подвески тени — 15% от реального.** `docs §28` даёт реальный ODE `m_suspensionRange =
0.600 м`. Тень строит `mSuspensionMaxLength = 0.05 + susp_max_scale * range` при
`jolt_wm4_susp_max_scale = 0.15` (`joltshadow.cpp:3186-3187`) → рабочий ход **0.090 м**, из которых
на покое уже израсходовано 0.035 м (78% хода сжатия). У Mirotvorec самый большой реальный ход во
флоте, значит и самое большое абсолютное урезание (0.600 → 0.090). Этот `0.15` выбран не здесь и не
для этого: он из задачи #59 (ошибка высоты корпуса Ural01), и в комментарии `config.hpp:116-124`
прямо записано, что это компромисс, который «стоит реального хода подвески», а околонулевое
значение даёт «ощущение неразрезного моста». То есть тень едет на почти жёсткой подвеске ПО
ПРОЕКТУ, и это ровно то условие, при котором скребущий бинд некуда девать.

**(5) Сил хватает с запасом — они гасят друг друга.** Вес машины 4032 Н (411 кг, из `docs §101`).
Подвеска несёт ~525 Н на колесо, доступное трение `D≈625 Н` на колесо → ~3750 Н на 411 кг ≈ 9 м/с².
Тяги хватило бы разогнать машину бодро. Контакт здоровый (6/6 колёс, 12 точек, `pen/tau=0.17`, ни
одного упора). Корпус не спит. Значит дело не в нехватке сцепления/нагрузки, а в том, что
результирующая близка к нулю.

**(6) Знак рулевого угла — не наша выдумка.** `joltshadow.cpp:4656-4677`: цель мотора — реальный
`hw->m_curAngle` КАЖДОГО колеса из ODE, уже с учётом знака, ограничения скорости и фактически
достигнутого угла. Ошибка Ackermann-геометрии на нашей стороне исключена: мы зеркалим то, что
делает эталон.

**Складывая (2)-(5)**: тень с почти жёсткой подвеской, стартуя из холодного покоя, при полном руле
на двух разнесённых рулевых мостах попадает в переопределённую конфигурацию, где силы колёс
взаимно гасятся; больше итераций солвера удерживает эту конфигурацию ТОЧНЕЕ (§139.10 — не парадокс,
а подпись переопределённости). После разгона по прямой машина в эту конфигурацию не попадает.

## §140.2. План

Порядок — от самого дешёвого и решающего к дорогому. Каждый шаг сначала МЕРЯЕТ, потом решает.

**Шаг 1. Починить разрешение измерения (иначе все шаги ниже слепы).**
Поднять точность COM в строке `Shadow divergence` (`LogDivergence`, формат `%.1f` → `%.3f`).
Это одна строка формата, не физика: ничего не меняет в поведении, но без неё «стало 0.7 м вместо
0.0 м» неотличимо от шума квантования. Заодно проверить, не считает ли `analyze()` в
`fleet_calibration_sweep.py` ещё где-то по квантованным значениям.
*Критерий*: повтор одного и того же прогона даёт РАЗНЫЕ значения `jolt_travel_m` в 3-м знаке
(т.е. измеряется физика, а не сетка формата).

**Шаг 2. Зафиксировать реальные условия отказа (n≥3, не n=1).**
Матрица на `Mirotvorec01`: {руль сразу после загрузки} × {руль после прямого разгона} × n=3.
Плюс контроль на `Cruiser01` и `Tank01` (та же топология 4 рулевых) в обоих режимах.
*Зачем*: это отделяет «беда из-за топологии» от «беда из-за холодного старта». Пока данные
говорят, что решает именно холодный старт, но это n=1 против n=12 в разных условиях — надо
свести в одну честную таблицу.
*Критерий*: воспроизводимость отказа названа числом (например «застревает 3/3 на холодную,
0/3 после разгона»), а не одним прогоном.

**Шаг 3. Прямая проверка «переопределённости» на живых числах.**
На залипшем кадре снять по каждому колесу: командный угол, фактический угол, боковое
проскальзывание, направление силы в плоскости земли. Посчитать подразумеваемый центр поворота
отдельно для переднего и среднего моста. Часть данных уже логируется (`docs §125` даёт
`v_par`/`v_lat`), угла и вектора силы на колесо — нет, добавить в существующую диагностическую
строку (только лог, без изменения физики).
*Критерий*: либо видно, что силы переднего и среднего мостов направлены навстречу и сопоставимы
по модулю (гипотеза подтверждена), либо не видно (гипотеза снята и план переписывается по факту).

**Шаг 4. A/B хода подвески — БЕЗ пересборки, только ini.**
`wm4_susp_max_scale`: 0.15 (как есть) / 0.40 / 1.00 на репро из шага 2, n≥3.
*Ожидание*: если бинд не поглощается из-за жёсткости, рост хода должен расстопорить машину.
*Важно*: даже положительный результат НЕ означает «поменять дефолт на 1.0» — 0.15 держит
задачу #59 (высота корпуса Ural01). Положительный результат означает «найден настоящий конфликт
двух требований», и тогда работает шаг 5. Отрицательный — подвеска ни при чём, идём в шаг 6.

**Шаг 5 (условный, если шаг 4 сработал). Снять сам конфликт, а не выбирать сторону.**
Оба симптома (ошибка высоты корпуса #59 и урезанный ход) растут из одного корня: **у ODE вообще
нет жёстких упоров подвески** — это чистое CFM/ERP-мягкое ограничение (зафиксировано в
`joltshadow.cpp` собственным комментарием), а порт вынужден был выдумать `[minLen, maxLen]`, потому
что SixDOF требует пределов. Правильный ремонт — не крутить глобальный скаляр, а привести модель к
эталону: держать упоры заведомо вне рабочего диапазона (или убрать их в пользу мягкого
ограничения) и чинить высоту корпуса там, где она реально задаётся — длиной покоя/геометрией
крепления, а не сжатием хода. Это лечит и Mirotvorec, и первопричину #59, и, возможно, задевает
#56 (Ural01). Оформляется отдельным под-планом с собственными шагами и флагом, по дисциплине
проекта — не «заодно».

**Шаг 6 (условный, если подвеска ни при чём). Мягкий выход из переопределённости.**
Тогда лечить надо саму несовместимость: у эталона её поглощает мягкий контакт ODE, у нас —
жёсткая штрафная шина (`k_t=120000 Н/м`). Кандидаты (мерить, не угадывать): понижение
эффективной боковой жёсткости в зоне очень малых скоростей; корректная зона stick→slip перехода в
`wheelmodel_core` при комбинированном проскальзывании. Это работа над моделью трения, влияющая на
ВЕСЬ флот — со своим планом, флагом и флотским замером, как §124.

**Шаг 7. Верификация.** Репро из шага 2 → чисто, n≥3. Затем флотский прогон 23 машин ×2 сценария
(без регрессий), затем — живое подтверждение пользователем за рулём, по стандарту проекта
(автометрика необходима, но §124.8 уже показал, что её одной не хватает).

## §140.2a. Шаг 1 выполнен: точность лога поднята, ворота пройдены

`LogDivergence` (`joltshadow.cpp:3721`) печатал обе тройки COM форматом `%.1f`; заменено на `%.3f`
с комментарием, объясняющим, что именно этот формат породил ложный вывод §139.8. Изменение
чисто логовое, физику не трогает; regex-разбор в `fleet_calibration_sweep.py` (`[\-\d. ]+`) новый
формат принимает без правок. Собрано, задеплоено.

*Ворота пройдены*: три повтора одной и той же конфигурации дали `0.756 / 0.798 / 0.791` вместо
прежних одинаковых `0.7615773105865042` — значение теперь разрешает физику, а не сетку формата.

## §140.2b. Шаг 2 выполнен: отказ — это ХОЛОДНЫЙ СТАРТ, и он не только у Mirotvorec

Матрица {холодный старт} × {после прямого разгона} × n=3, плюс `Cruiser01`/`Tank01` как контроль
той же топологии (2 рулевых моста):

| Конфигурация | `jolt_travel_m` (n=3) | застряло (<2 м) |
|---|---|---|
| **Mirotvorec01, холодный** | 0.756, 0.798, 0.791 | **3/3** |
| **Mirotvorec01, после разгона** | 16.33, 9.52, 9.86 | 0/3 |
| Cruiser01, холодный | 3.52, 3.48, 3.65 | 0/3 |
| Cruiser01, после разгона | 28.57, 32.75, 24.28 | 0/3 |
| Tank01, холодный | 6.11, 69.93, 9.13 | 0/3 |
| Tank01, после разгона | 35.23, 17.48, 35.40 | 0/3 |

Выводы, каждый — по числам, а не по одному прогону:

1. **Гипотеза §140.1(2) подтверждена начисто**: 3/3 против 0/3, без пересечения диапазонов
   (0.75-0.80 м против 9.5-16.3 м). Отказ вызывает не машина сама по себе, а старт рулевого
   манёвра из холодного покоя сразу после загрузки. Это же объясняет, почему §139.7 (флотский
   прогон, где рулю предшествовал прямой разгон) проблему не увидел, а §139.8 увидел.
2. **Эффект НЕ уникален для Mirotvorec01.** `Cruiser01` на холодную даёт 3.5 м против 24-33 м
   после разгона — падение примерно в 8 раз, воспроизводимое с разбросом менее 5% (3.52/3.48/3.65).
   Это не «одна сломанная машина», а общий механизм, у которого Mirotvorec просто самый тяжёлый
   случай. Значит и чинить надо механизм.
3. `Tank01` на холодную шумит (6.1/69.9/9.1) и однозначного сигнала не даёт — записываю как есть,
   без натягивания на гипотезу.
4. **Оговорка к «тёплой» половине таблицы**: там сам ЭТАЛОН местами почти не едет (у
   `Mirotvorec01` `ode_travel_m` = 0.775 и 1.415 м в двух прогонах из трёх) — после 155-метрового
   разгона машина оказывается там, где уже ODE не может развернуться. То есть «тёплый» режим —
   корректный контраст для тени, но плохая эталонная точка сам по себе. Для шага 7 нужен будет
   сценарий получше, чем «уехать 155 м и там крутить руль».

**Порядок шагов 3 и 4 меняю местами** (и это отступление от плана, поэтому фиксирую явно): шаг 4
не требует ни строчки кода (только ini) и почти так же решающий, поэтому дешевле выполнить его
первым и писать инструментовку шага 3 только если шаг 4 не даст ответа.

## §140.2c. Шаг 4 выполнен: ход подвески НИ ПРИ ЧЁМ — гипотеза §140.1(4) опровергнута

Холодный репро (подтверждённый шагом 2), n=3 на арм, только ini, без пересборки:

| Арм | что меняет | `jolt_travel_m` (n=3) | застряло |
|---|---|---|---|
| `scale 0.15 / frac 0.50` | как в поставке (контроль) | 0.775, 0.801, 0.788 | 3/3 |
| `scale 0.15 / frac 0.85` | **только** запас сжатия (высота посадки не меняется) | 0.762, 0.801, 0.767 | 3/3 |
| `scale 0.40 / frac 0.50` | ход ×2.7 | 0.800, 0.805, 0.776 | 3/3 |
| `scale 1.00 / frac 0.50` | полный реальный ход ODE (×6.7) | 0.752, 0.805, 0.777 | 3/3 |

**Ноль эффекта.** Все четыре распределения неотличимы (0.75-0.81 м), включая арм с ПОЛНЫМ реальным
ходом подвески ODE. Причём чистый арм `frac=0.85` (больше запаса сжатия при неизменной высоте
посадки — специально введён, чтобы не смешивать «ход» с «высотой») тоже не дал ничего, так что
отрицательный результат не замаскирован конфаундом.

**Следствия, которые это закрывает:**
- Гипотеза §140.1(4) («почти жёсткая подвеска не даёт поглотить скребущий бинд») — **опровергнута**.
  То, что тень едет на 15% реального хода, остаётся фактом и, возможно, портит другие вещи, но
  ЭТОТ отказ вызывает не оно.
- **Шаг 5 плана снимается** как средство от этого бага. Это самая дорогая ветка плана (переделка
  упоров подвески всего флота, задевающая закрытую #59 и открытую #56) — и хорошо, что она отпала
  по измерению за 12 прогонов ini-A/B, а не после недели работы. Как отдельная задача «привести
  подвеску к безупорной модели ODE» она может быть осмысленной, но к Mirotvorec отношения не имеет.
- Остаётся ветка шага 6 (модель трения / вход в stick-slip у неподвижной машины), плюс новый
  разделяющий эксперимент ниже.

## §140.2d. Что именно даёт «прогрев»: не рельеф, а вход в манёвр из неподвижности

Шаг 2 доказал зависимость от холодного старта, но «прогрев» менял сразу три вещи: место на карте,
факт качения и темп подачи руля. Разделяющий эксперимент (всё из того же холодного сейва, n=3):

| Арм | Что делает | `jolt_travel_m` (n=3) | застряло |
|---|---|---|---|
| A `cold_instant` | контроль: газ+полный руль с места | 0.771, 0.763, 0.764 | **3/3** |
| B `roll_2s` | 2 с прямого газа (несколько метров, тот же участок), потом руль | 15.01, 13.13, 13.84 | 0/3 |
| C `steer_ramp` | газ с места, руль ступенькой 0→0.25→0.5→1 за 2 с | 6.76, 6.67, 6.79 | 0/3 |

1. **Рельеф/место исключены.** Арм B отъезжает всего несколько метров по тому же участку — и этого
   достаточно. Арм C **вообще не покидает точку спавна**, меняется ТОЛЬКО темп подачи руля, и
   залипание исчезает 3/3. Значит дело не в том, ГДЕ машина, а в том, КАК она входит в манёвр.
2. **Триггер назван точно**: мгновенная подача полного руля при нулевой скорости. Достаточно либо
   успеть покатиться до поворота (B), либо доводить руль постепенно (C).
3. Разброс внутри армов крошечный (C: 6.67-6.79 м, менее 2%) — эффект детерминированный, не шумовой.
4. Честная оговорка: даже расстопорившись, тень заметно недоворачивает относительно ODE (ratio
   0.35-0.40 у B, 0.26-0.30 у C). Это уже задача калибровки §139, а не залипания; смешивать их не
   надо.

**Механизм, найденный в коде** (`wheelmodel_core.hpp:245-250`, прочитан, не угадан):

```
stick     = clamp(1 - |Dot(v_p, t)| / stick_speed, 0, 1)   // по скорости ДВИЖЕНИЯ МАШИНЫ
kStick    = 2 * (2 * sqrt(k_t * m))
damperPar = clamp(-v_par * kStick, -D, D)                  // по ПРОСКАЛЬЗЫВАНИЮ колеса
f_par     = lerp(f_par, damperPar, stick)
```

Два свойства делают это ловушкой именно для старта с места:

- `stick` считается по скорости ДВИЖЕНИЯ машины, а `damperPar` — по ПРОСКАЛЬЗЫВАНИЮ колеса.
  Неподвижная машина с вращающимися колёсами сидит на `stick≈1`, то есть реальная сила Пасейки
  почти целиком подменена демпфером — и так до тех пор, пока машина не поедет. Петля
  самоподдерживающаяся: «не еду» держит демпфер включённым, а демпфер не даёт поехать.
- При `k_t=120000` коэффициент `kStick` таков, что `damperPar` упирается в свой ограничитель `±D`
  уже на единицах см/с проскальзывания, то есть ведёт себя как разрывная bang-bang цель — ровно то,
  что собственный комментарий этого кода (§125 / stage2-plan.md §2.9) описывает как «chattered
  violently» под явным интегрированием; двойной коэффициент выбирался как компромисс против этого.

Гипотеза объясняет ВСЕ наблюдения разом: залипание на холодную и его отсутствие в движении
(§140.2b, §140.2d), независимость от хода подвески (§140.2c), схождение к нулю при росте итераций
солвера (§139.10 — демпфер гонит v→0, и больше итераций исполняют это точнее), присутствие сил при
нулевой равнодействующей и смену знака `omega` каждые 1-2 кадра (§139.9).

## §140.2e. Шаг 5: липкий демпфер — участник, но НЕ вся причина (частичное подтверждение)

Прямая проверка механизма из §140.2d через `[wheelmodel] stick_speed` (окно, в котором демпфер
подмешивается), холодный репро, n=3:

| `stick_speed` | `jolt_travel_m` (n=3) | застряло |
|---|---|---|
| 0.50 (как в поставке) | 0.773, 0.808, 0.754 | 3/3 |
| 0.10 | 2.343, 2.236, 2.425 | 0/3 |
| 0.01 | 2.341, 2.373 (третий прогон — таймаут) | 0/2 |

**Честный вердикт — частичное подтверждение.** Сужение окна даёт ровно втрое больше хода
(0.78 → 2.35 м) и снимает формальный критерий залипания, то есть демпфер действительно участвует и
механизм §140.2d реален. Но:

- нормальное поведение НЕ восстанавливается: армы B/C из §140.2d давали 13-15 м и 6.7 м, здесь
  потолок 2.4 м;
- значения 0.10 и 0.01 дают ОДИН И ТОТ ЖЕ результат — эффект насыщается, дальше сужать бессмысленно;
- то есть демпфер объясняет примерно троекратную часть примерно восемнадцатикратного провала.
  Рядом работает второй механизм, и он доминирующий.

Отдельно отмечаю таймаут одного прогона на `stick_speed=0.01` — единичный, в отчёт занесён как
есть, отдельно не расследовался.

**Важно про возможный «фикс»**: крутить `stick_speed` как решение НЕЛЬЗЯ. Это значение держит
задачу #60 (припаркованная машина не должна ползти по склону, §125.2), и оно там появилось после
долгой бисекции. Даже если бы оно чинило залипание полностью, правкой был бы не сдвиг этого числа,
а исправление самого критерия: `stick` считается по скорости КУЗОВА, тогда как подменяемая им сила
зависит от ПРОСКАЛЬЗЫВАНИЯ. Пробуксовка под газом у стоящей машины — законное физическое
состояние, а код принимает её за покой. Правильная правка (когда дело дойдёт) — сделать критерий
зависимым и от проскальзывания тоже (произведение двух окон), чтобы припаркованный случай
(и то, и другое ~0) сохранился, а буксующий отпускал демпфер. Флаг + флотский замер обязательны.

## §140.2f. Шаг 6: путь подачи руления тоже НИ ПРИ ЧЁМ

`[jolt_harness] wm4_steer_kinematic` переключает механизм подачи угла: 0 — Position-мотор с
нагрузочным потолком момента, 1 — тот же мотор с кинематическим (огромным) потолком, 2 —
буквальное ПРИСВАИВАНИЕ ориентации колеса (порт `_TurnWheelByAngle`, дефолт поставки). Холодный
репро, n=3:

| `wm4_steer_kinematic` | `jolt_travel_m` (n=3) | застряло |
|---|---|---|
| 2 (присваивание, дефолт) | 0.813, 0.787, 0.796 | 3/3 |
| 1 (мотор, большой потолок) | 0.610, 0.607, 0.747 | 3/3 |
| 0 (мотор, нагрузочный потолок) | 0.734, 0.720, 0.746 | 3/3 |

Ноль эффекта — залипает во всех трёх. Это был мой собственный главный подозреваемый (раз триггер
— мгновенность подачи, логично было винить путь, который телепортирует ориентацию колеса), и он
опровергнут измерением. Записываю как есть.

## §140.2g. НАЙДЕНО: угол руления у ODE и у тени РАЗНЫЙ — перекос всей методики §139

Разбирая, откуда вообще берётся «мгновенный полный руль», нашёл кое-что важнее самого залипания.
Три факта, все прочитаны в исходниках:

1. **`testharness.cpp:558`**: `wheel->m_curAngle = sign * sample->steer;` — стенд пишет угол
   поворота колеса НАПРЯМУЮ, каждый тик, сознательно обходя гонку за `m_steerRadians` (об этом
   говорит его собственный комментарий, задача #34). То есть значение `steer` из сценария
   попадает в `m_curAngle` **как радианы**.
2. **Штатный путь так не делает**: `ai::Vehicle::_KeepSteer` доводит `m_curAngle` до цели
   ПОСТЕПЕННО, со скоростью `m_steeringSpeed` (у `Mirotvorec` `SteeringSpeed="0.6"`), вызывая
   `_TurnWheelByAngle` с дельтой. Живой игрок физически не может получить скачок угла за один кадр.
3. **Величина выходит за собственный предел игры**: командный предел руления —
   `Wheel::STEERING_LIMIT = pi/4 = 0.7853982` рад (`joltshadow.cpp:323`, константа прочитана из
   бинарника по адресу `0x5931f4`). Сценарный `steer=1.0` просит **1.0 рад = 57.3°**, то есть ~127%
   от полного упора игры. При этом тень ОБРЕЗАЕТ прочитанное до `±0.785` (`joltshadow.cpp:4675-4676`),
   а эталону достаётся сырое значение.

**Два независимых следствия:**

**(а) Триггер залипания недостижим в реальной игре.** Мгновенная подача полного руля — артефакт
стенда; штатный ограничитель скорости поворота растягивает её примерно на 1.7 с, что близко к арму
C из §140.2d, а тот проходит чисто 3/3. Значит `Mirotvorec01`, скорее всего, НЕ сломан за рулём у
игрока, а сломан сценарий. Это надо подтверждать живой ездой (стандарт проекта: телеметрии одной
мало, §124.8), а не считать доказанным по этому рассуждению.

**(б) Потенциально более серьёзное: два движка ведут разный угол.** Если ODE отрабатывает ~1.0 рад,
а тень — обрезанные 0.785 рад, то тень недоворачивает **по построению**, примерно на 27%, в КАЖДОМ
рулевом замере §139. А систематическое недоворачивание — ровно то, что §139 всю дорогу гонялся
закрыть глобальными скалярами трения (у большинства машин `ratio` ниже 1.0). Скаляр, подогнанный
под компенсацию методической ошибки, — это подгонка под артефакт, а не калибровка.

Проверяю (б) отдельным замером: та же сцена с амплитудой `steer=0.785` (внутри предела игры,
обрезать нечего) против нынешней `1.0`, на машинах, которые едут нормально (`Bug01`, `Cruiser01`,
`Molokovoz01`), n=2. Если при 0.785 `ratio` подрастает к 1.0 — перекос реален и все рулевые числа
§139 надо перемерять. Если не меняется — значит ODE где-то обрезает угол сам, и числа §139 в силе.
Результат — ниже, каким бы он ни был.

## §140.2h. Шаг 7: перекос угла РЕАЛЕН, но сильно зависит от машины

Та же сцена, амплитуда `steer=1.0` (как во всех замерах §139) против `0.785` (= π/4, внутри
собственного предела игры — обрезать нечего), n=2:

| Машина | амплитуда | `jolt_travel_m` | `ode_travel_m` | ratio |
|---|---|---|---|---|
| Bug01 | 1.000 | 7.99, 8.31 | 14.40, 14.65 | 0.555, 0.567 |
| Bug01 | 0.785 | 8.02, 6.55 | 16.32, 14.58 | 0.491, 0.449 |
| Cruiser01 | 1.000 | 3.84, 3.64 | 16.00, 15.86 | 0.240, 0.229 |
| Cruiser01 | 0.785 | 3.52, 3.73 | 16.14, 17.71 | 0.218, 0.211 |
| **Molokovoz01** | **1.000** | 7.49, 8.08 | **38.43, 39.41** | 0.195, 0.205 |
| **Molokovoz01** | **0.785** | 9.70, 4.55 | **11.61, 2.45** | **0.836, 1.856** |

**Моё направленное предсказание не сбылось** (я ждал, что ratio у всех подрастёт к 1.0), и это
надо сказать прямо. Но сама асимметрия подтвердилась — по другому признаку, дифференциальному:

- У `Molokovoz01` ЭТАЛОН реагирует на смену амплитуды драматически: 38.4/39.4 м при 1.0 рад против
  11.6/2.4 м при 0.785 рад. То есть при 1.0 рад ODE едет почти прямо, а при 0.785 — плотной дугой.
  Тень же в обоих случаях идёт примерно одинаково (7.5-8.1 против 4.5-9.7). Ratio из-за этого
  скачет 0.20 → 0.84/1.86, то есть в 4-9 раз. Это ровно та картина, которую даёт обрезание на
  нашей стороне: тень уже сидела на 0.785 в обоих случаях и смены не заметила, эталон заметил.
- У `Bug01` и `Cruiser01` эффект в пределах шума (n=2), обе стороны почти не изменились.

**Вывод, честно и с границами.** Перекос реален и механизм подтверждён чтением исходников
(§140.2g), но его ВЛИЯНИЕ резко зависит от машины: для `Molokovoz01` оно решающее, для
`Bug01`/`Cruiser01` — в пределах шума. Значит:

1. Рулевые `ratio` в §139 **не являются чистым сравнением движков для всех машин** — как минимум
   для части флота два движка ехали с разными углами колёс. Подгонять глобальные скаляры трения
   под такие числа — подгонка под артефакт методики.
2. Разброс у `Molokovoz01` при 0.785 огромный (11.6 против 2.4 м между двумя прогонами), так что
   и n=2 тут мало; полноценный вывод требует n≥3-5.
3. Это НЕ объясняет залипание `Mirotvorec01`: обрезание одинаково работает и в застревающем, и в
   рабочем арме, а между ними разница только в темпе подачи.

**Что из этого следует чинить.** Писать в `m_curAngle` значение за пределом собственного лимита
игры (`Wheel::STEERING_LIMIT = π/4`) — это фабрикация недостижимого состояния; стенд не должен так
делать. Отдельный вопрос — надо ли стенду ещё и ограничивать СКОРОСТЬ поворота, как это делает
`_KeepSteer`: это ближе к реальности, но меняет смысл всех существующих сценариев, поэтому
требует отдельного решения, а не тихой правки заодно.

## §140.2i. ГЛАВНОЕ: это не «не может тронуться», а ОСТАНОВКА В УПОРЕ РУЛЯ. Мои §140.2b/§140.2d переформулированы

Снял одинаковую покадровую диагностику с ДВУХ прогонов — рабочего (рампа) и застревающего
(мгновенный руль), одна машина, один сейв, одна точка, один формат. Сравнение убивает мою же
предыдущую формулировку.

**Сначала неожиданное**: по колесу 0 в установившейся фазе оба прогона почти НЕРАЗЛИЧИМЫ —
`v_par` медиана 0.640 против 0.614, `stick` 0.402 против 0.434, `capPar` 553 Н против 512 Н, и
смен знака `v_par` в РАБОЧЕМ прогоне даже БОЛЬШЕ (41% против 28%). То есть «дребезг» из §139.9
присутствует в обоих одинаково и **не является тем, что их различает**. Диагноз §139.9 как причины
— снят.

**Что различает — скорость кузова по времени** (средняя по бинам 0.5 с; газ подаётся на t≈3.0 с,
рампа доводит руль до упора к t≈5.0 с):

| t, с | рабочий (рампа) | застрявший (мгновенно) |
|---|---|---|
| 3.0 | 0.36 | 0.04 |
| 3.5 | **3.13** | 0.10 |
| 4.0 | **4.60** | 0.11 |
| 4.5 | **4.04** | 0.10 |
| **5.0** (руль дошёл до упора) | **1.47** | 0.10 |
| 5.5 | **0.13** | 0.10 |
| 6.0 … 12.0 | 0.10 | 0.10 |

**Рампа ничего не чинит.** Она даёт машине разогнаться до 4.6 м/с, пока руль ещё не в упоре, и
все её 7 м набираются за эти две секунды. Ровно в момент достижения упора скорость обрушивается:
4.04 → 1.47 → 0.13 м/с примерно за одну секунду, и дальше прогон неотличим от «застрявшего».
Подтверждается и вращением колеса: в рабочем прогоне `wheelAngVel` доходит до 1.755 рад/с, в
застрявшем — 0.319 рад/с, а к концу оба одинаковы.

**Переформулировка диагноза.** Это НЕ «не может тронуться из неподвижности» (§140.2b) и НЕ
«триггер — мгновенность подачи» (§140.2d). Правильно так: **при полном упоре руля Jolt-тень
`Mirotvorec01` останавливается насмерть, с любой скорости.** Машину на 4.6 м/с полный руль тормозит
до 0.1 м/с за секунду. Всё, что выглядело как «зависимость от холодного старта» и «зависимость от
темпа подачи», — это просто разное количество метров, успетых набрать ДО того, как упор включился.
Обе прежние формулировки оставляю в документе с этой пометкой, не переписывая: проект фиксирует
опровергнутые выводы, а не прячет их.

**Что это означает механически.** Возвращает в игру гипотезу §140.1(3), которую я преждевременно
отложил: у `Mirotvorec01` рулят ДВА моста на разном продольном удалении (передний `foreAft=+3.6`
и средний `foreAft=0.0`), и оба получают ОДИН И ТОТ ЖЕ угол. Геометрически это требует разных
центров поворота — конфигурация переопределена, шины обязаны скрести. У эталона ODE та же
геометрия и те же одинаковые углы (`m_curAngle` общий), но ODE проезжает 13-23 м: его мягкий
CFM/ERP-контакт позволяет шине проскальзывать сквозь несовместимость. Наша жёсткая штрафная шина
(`k_t=120000 Н/м`) вместо этого запирается. Это же объясняет и §139.10 (больше итераций солвера →
точнее удерживается запертая конфигурация → МЕНЬШЕ движения), что раньше выглядело парадоксом.

**Границы утверждения.** Что тень запирается в упоре — измерено надёжно (по двум независимым
прогонам с покадровой телеметрией). Что причина именно в геометрической несовместимости двух
рулевых мостов — пока лучшая объясняющая гипотеза, согласующаяся со всеми измерениями, но прямо
НЕ доказанная: для доказательства нужно снять по каждому колесу вектор силы в плоскости земли и
показать, что вклады переднего и среднего мостов направлены навстречу (это шаг 3 исходного плана,
он требует новой диагностической строки в коде). До этого — гипотеза, а не факт.

## §140.2j. ДОКАЗАНО: 96% продольной силы гасится между мостами. Корневая причина названа

Шаг 3 исходного плана выполнен. Добавлена диагностика (только лог, физика не тронута): в
`wheelmodel_core.hpp` `WMForce` теперь отдаёт `dbg_f_par`/`dbg_f_lat` — силы ПОСЛЕ круга трения,
клэмпов и stick-подмешивания, то есть ровно те, что уходят в `Fvec`; в `joltshadow.cpp` они вместе
с продольной станцией колеса (`foreAft`, считается так же, как эталонная в `docs §28`) пишутся
новой строкой `docs §140: wheel force`. Проверка сходимости: `foreAft` совпал с эталонными
данными ODE колесо в колесо (`w0,w1=+3.60` перед, `w4,w5=0.00` середина, `w2,w3=-3.60` зад).

Оба прогона пересняты с этой диагностикой (~490 полных 6-колёсных кадров каждый, установившаяся
половина):

| Колесо (мост) | ЗАСТРЯВШИЙ `f_par` | РАБОЧИЙ `f_par` |
|---|---|---|
| w0 (перед +3.6) | **+39.5 Н** | −125.5 Н |
| w1 (перед +3.6) | **+47.2 Н** | −172.6 Н |
| w4 (середина 0.0) | −2.3 Н | −207.2 Н |
| w5 (середина 0.0) | **+47.1 Н** | −198.4 Н |
| w2 (зад −3.6) | **−67.1 Н** | −136.6 Н |
| w3 (зад −3.6) | **−191.7 Н** | −194.4 Н |
| **валовая Σ\|f_par\|** | **2874 Н** | 1383 Н |
| **чистая Σ f_par** | **−127 Н** | −1035 Н |
| **взаимное погашение** | **96%** | **25%** |

**Это и есть корневая причина, теперь измеренная, а не предположенная.** В застрявшем состоянии
колёса развивают ВДВОЕ большую суммарную силу, чем в рабочем (2874 против 1383 Н), и 96% её
уходит на борьбу друг с другом: передний мост толкает в плюс, задний — в минус, равнодействующая
−127 Н на машину массой 411 кг, то есть около 0.3 м/с² — при доступных ~9 м/с². В рабочем прогоне
все шесть колёс толкают согласованно в одну сторону и гасится лишь четверть.

Уточнение к гипотезе §140.1(3): противостояние оказалось не «передний против среднего» (оба
рулевых), как я предполагал, а **рулевые мосты против НЕРУЛЕВОГО заднего**. Механизм: при полном
упоре геометрия требует очень тесной дуги, задняя ось при этом обязана скользить вбок; у эталона
мягкий CFM/ERP-контакт ей это позволяет, а наша жёсткая штрафная шина (`k_t=120000 Н/м`) вместо
проскальзывания выдаёт большую встречную продольную силу — и машина запирается сама об себя.

Это замыкает всю цепочку наблюдений без остатка: почему больше итераций солвера дают МЕНЬШЕ
движения (§139.10 — точнее удерживается запертая конфигурация), почему ход подвески и путь подачи
руления ни при чём (§140.2c, §140.2f), почему сужение stick-демпфера даёт лишь троекратный выигрыш
и упирается в потолок (§140.2e — он не главный участник), и почему дребезг одинаков в обоих
прогонах (§140.2i — он следствие, а не причина).

**Куда идти за фиксом (и куда НЕ идти).** Чинить надо способность шины проскальзывать вбок под
большим углом увода — то есть поведение `Phi`/круга трения в зоне больших `alpha`, где эталон
отпускает, а мы держим. Это правка модели трения, влияющая на ВЕСЬ флот, поэтому: под флагом,
с флотским замером 23 машин до смены умолчания, и с обязательным живым подтверждением за рулём.
Чего делать нельзя: подгонять что-либо под `Mirotvorec01` или под «машины с двумя рулевыми
мостами» — причина общая, просто у этой геометрии она вылезает ярче всего
([[feedback_no_hardcoded_exceptions]]).

## §140.3. Риски и границы

1. **Шаг 5 трогает подвеску всего флота** — то есть ровно тот код, от которого зависят уже
   закрытые #59 и открытая #56. Только под флагом, только с флотским замером до смены дефолта.
2. **Шаг 6 — это переписывание модели трения**, прямо названное в исходном плане §139 (риск №2)
   как «намного больший rewrite, вне рамок». Если план дойдёт до него — это отдельный проект, а не
   продолжение калибровки.
3. **Соблазн частного фикса.** Любая правка «если у машины 2 рулевых моста, то ...» или подгонка
   под Mirotvorec — запрещена: причина общая (жёсткая подвеска + жёсткая шина + переопределённая
   геометрия), лечить надо механизм ([[feedback_no_hardcoded_exceptions]],
   [[feedback_no_runtime_patch_hacks]]).
4. **Шаг 1 обесценивает часть уже снятых чисел §139** — не все, но мелкие перемещения придётся
   перемерить. Это цена того, что квантование нашлось поздно; лучше признать, чем продолжать
   строить выводы на 0.1-метровой сетке.

## §140.4. Что уже сделано и не требует повторения

- Опровергнуто: сон тела, потеря контакта с землёй, нехватка бюджета солвера, отсутствие
  демпфирования Velocity-мотора (у Jolt `mSpringSettings` в этом режиме не используется вовсе),
  ошибка знака/расчёта рулевого угла на нашей стороне, нехватка массы/сцепления.
- Добавлен и оставлен рабочий рычаг `jolt_wm4_drive_vel_steps`/`wm4_drive_pos_steps` (§139.10) —
  дефолты равны прежнему литералу, поведение по умолчанию не менялось.

## §140.5. Задача #65 выполнена и измерена — ЧИСТЫЙ НОЛЬ

Сделано ровно то, что предписывал §140.2j: комбинированная модель скольжения под флагом
`[jolt_harness] wm4_iso_slip` (умолчание 0). Вместо двух независимых вызовов `Phi` — по
коэффициенту продольного скольжения и по УГЛУ увода — с последующим сжатием пары в круг трения,
строится ОДИН вектор теоретического скольжения `s = (-v_par/v_ref, -v_lat/v_ref)`, `Phi`
вычисляется один раз от `|s|`, а сила направляется вдоль `-ŝ`. Продольная доля становится
`s.x/|s|` и обнуляется по мере ухода скольжения в поперечное — то есть шина ОТПУСКАЕТ продольную
составляющую, а не выдумывает её.

Самопроверка (`SelfTest[7]`, гейт задачи #65, печатается каждый запуск) подтверждает, что правка
делает именно это и не задевает того, что задевать нельзя:

```
scrub f_par -2545N -> -211N, f_lat -2546N -> -3159N; straight f_par -3598N vs -3598N -> PASS
```

То есть при боковом волочении продольная сила падает в 12 раз, поперечная при этом РАСТЁТ (сцепление
перенаправлено, а не потеряно), а при чисто продольном скольжении (`v_lat = 0`) новая форма
тождественна старой до последнего ньютона — `s.y = 0`, `|s| = |s.x|`, `Phi` нечётна.

**Живой A/B на подтверждённом репро (`Mirotvorec01`, `save 00000009`, чередование арм, n=3):**

| арм | `jolt_travel_m` | `ode_travel_m` | застряло <2 м |
|---|---|---|---|
| `wm4_iso_slip=0` | 0.801, 0.785, 0.777 | 16.23, 14.07, 16.25 | 3/3 |
| `wm4_iso_slip=1` | 0.769, 0.759, 0.782 | 15.30, 13.25, 13.22 | 3/3 |

Разницы нет. Предсказание, записанное ДО прогона («арм 1 расклинит машину»), не сбылось.

Код оставлен под флагом с умолчанием 0, а не удалён: сама по себе комбинированная форма физически
корректнее раздельной (кулоновское трение противодействует ОДНОМУ направлению скольжения, а не двум
независимым), и она входит в набор рычагов для флотского свипа задачи #61. Но как фикс залипания
она опровергнута.

## §140.6. Отзыв §140.2j: «96% взаимного погашения» — артефакт замера

Ноль в §140.5 заставил перепроверить измерение, на котором строился вывод. Обе его опоры не выдержали.

**Первое: складывались числа из разных базисов.** `f_par` живёт в собственной касательной системе
каждого колеса, а у `Mirotvorec01` передний и средний мосты повёрнуты на 45°, задний — нет.
Сумма шести таких скаляров не является силой на корпусе, и часть «погашения» — это просто разница
систем координат. В строку `docs §140` добавлены поля `fwd`/`right` — та же сила, спроецированная
на оси КОРПУСА; складывать можно только их.

**Второе: контроль не был контролем.** «Рабочий» (рамповый) прогон при разбивке по скорости шасси
идёт быстрее 2 м/с всего ~1.2 с, а остальное время стоит ниже 0.5 м/с — он набирает свои метры в
самом начале и запирается ровно так же. Два запертых прогона не могут изолировать запирание. При
единой сетке кадров «погашение» у обоих одинаково (73% и 73%), никаких 96% против 25%.

**Что показал корректный замер** (`wm4_diag_interval=3`, 490 кадров, разбивка по скорости шасси):

| фаза | Σ`fwd` | Σ\|`fwd`\| | Σ`right` | a = Σ`fwd`/411кг |
|---|---|---|---|---|
| ползёт 0.05–0.5 м/с | **−1837 Н** | 2007 Н | +884 Н | **−4.47 м/с²** |
| стоит <0.05 м/с | 0 Н | 1933 Н | +524 Н | 0 |

Контакт при этом здоров у всех шести колёс (`ground≈336Н`, `pen=0.0028`), машина НЕ вращается на
месте (рыскание 0.5 °/с, за прогон 6.1° суммарно) и просто ползёт со скоростью 0.10 м/с.

Баланс не сходится: сила в −1837 Н, приложенная восемь секунд к телу в 411 кг, обязана разогнать
его до десятков м/с назад. Этого не происходит. **Значит сила, которую сообщает модель шины, — не
та сила, которую получает корпус.** Виновата не кривая трения, а слой её приложения.

## §140.7. Настоящее место дефекта: путь приложения силы (§124), а не модель шины

Прямая проверка предположения из §140.6 — тот же репро, `[jolt_harness] wm4_contact_constraint`
1 против 0, чистый ini, без пересборки, чередование арм, n=3:

| арм | `jolt_travel_m` | `ode_travel_m` | застряло <2 м |
|---|---|---|---|
| `wm4_contact_constraint=1` (текущий дефолт) | 0.755, 0.764, 0.777 | 15.27, 14.80, 15.36 | **3/3** |
| `wm4_contact_constraint=0` | 55.84, 51.26, 0.293 | 15.18, 16.51, 0.357 | **1/3** |

Залипание принадлежит пути `WheelContactConstraint` (§124, шаг 4: трение внутри констрейнта,
по итерации солвера), а не `wheelmodel_core`. Со старым путём машина едет.

Две честные оговорки, обе против поспешного вывода «вернуть 0»:
1. Арм 0 не «правильный», а противоположно неправильный: 55.8 и 51.3 м против эталонных 15.2 и
   16.5 — перелёт в 3.1–3.7 раза. Оба пути мимо, просто в разные стороны.
2. Третий прогон арма 0 дал 0.293 м при `ode_travel=0.357` — там не поехал и ЭТАЛОН, то есть это
   сбой запуска/сценария, а не результат арма. Записано как есть, из среднего не вычищено.

**Куда идти дальше.** Вопрос теперь узкий и проверяемый: почему сила, которую `WheelContactConstraint`
рапортует в `docs §140`, не превращается в ускорение корпуса. Кандидаты по порядку дешевизны
проверки: (а) сила прикладывается к телу колеса и гасится SixDOF-констрейнтом до того, как дойдёт
до шасси; (б) рапортуемое значение — величина одной итерации солвера, а не итог кадра, и тогда врёт
диагностика, а не физика; (в) знак/базис при переносе в констрейнт. Отличить (а) от (б) можно без
пересборки — сравнив Σ`fwd` с фактическим изменением импульса шасси за кадр.

**Что при этом НЕ подтвердилось и не должно повторяться**: «жёсткая шина не даёт задней оси
проскользнуть» (§140.2j) как причина. Формулировка звучала связно и объясняла всё — и оказалась
построена на сложении разнобазисных чисел и на контроле, который контролем не был.

## §140.8. Найдено: сила прикладывается 20 раз за шаг, а в лог пишется совсем другая величина

Задача #66. Два факта, оба из измерения.

**Факт 1: `SolveVelocityConstraint` вызывается ровно 20 раз за шаг, и каждый раз прикладывает
ПОЛНЫЙ импульс `Ftang*dt`.** Считать ничего не пришлось — счётчики уже были в строке `docs §124.7`:
`WarmStartVelocityConstraint` вызывается один раз за шаг, `SolveVelocityConstraint` — раз на
итерацию, и за прогон `solveCalls/warmStartCalls = 20.00` (29340 против 1467). В
`ApplyFrictionForSlot` нет ни накопленного `lambda`, ни ограничения приращения: каждая итерация
заново берёт силу от текущей скорости и добавляет `Ftang*dt` через `AddLinearVelocityStep`.
Это не PGS-итерация (там приращение импульса стремится к нулю по мере сходимости), а повторное
приложение полной силы.

**Факт 2: то, что писалось в `docs §140`, — это вообще не то, что прикладывает констрейнт.**
Поля `f_par`/`f_lat`/`fwd`/`right` заполняются из ОТДЕЛЬНОГО вызова `GeneralizedContactForce`
внутри `OnStep` — диагностического пересчёта, а не из `WheelContactConstraint`. Добавлены поля
`impFwd`/`impRight`: реальный импульс, накопленный констрейнтом за целый предыдущий шаг и делённый
на `dt`, то есть в тех же ньютонах. Расхождение не косметическое (медианы, фаза ползания):

| колесо | `foreAft` | `fwd` (пересчёт) | `impFwd` (реально) | `right` | `impRight` |
|---|---|---|---|---|---|
| 0 | +3.60 | −382 Н | **+2776 Н** | +184 Н | +1102 Н |
| 1 | +3.60 | −0 Н | −272 Н | +461 Н | **−1955 Н** |
| 2 | −3.60 | −440 Н | +370 Н | −437 Н | **+3523 Н** |
| 3 | −3.60 | −443 Н | +342 Н | −211 Н | **+2156 Н** |
| 4 | 0.00 | −292 Н | +154 Н | +110 Н | −468 Н |
| 5 | 0.00 | −0 Н | **−4118 Н** | +538 Н | **−4623 Н** |

Знак противоположен у большинства колёс, величина расходится до десяти раз. Реально приложенные
силы достигают ±4600 Н НА КОЛЕСО — при том, что вся машина весит 4032 Н. Колёса ведут между собой
силовую драку на порядок выше собственного веса машины, а сумма при этом почти гасится (Σ`impFwd`
= +570 Н против Σ`fwd` = −540 Н), и машина ползёт.

**Следствие для всего, что измерялось раньше.** Каждое число «силы», процитированное в §139 и
§140 — включая §140.6, где я же и утверждал, что «баланс не сходится», — описывало пересчёт, а не
симуляцию. Баланс действительно не сходится, но не потому, что сила теряется по дороге к корпусу:
в лог просто писалась не та сила. Это делает вывод §140.6 верным по факту («рапортуемая сила — не
то, что получает корпус») и неверным по механизму.

**Почему §138 не противоречит.** Там (задача #60) пробовали гейтить трение на ОДНУ итерацию и
получили не улучшение, а разнос `omega`; из этого был сделан вывод «значит это не
переприкладывание, а механизм удержания». Измерение выше показывает, что переприкладывание
всё-таки есть и оно десятикратное. Оба наблюдения совместимы: одно приложение той же СЫРОЙ силы
слишком слабо, чтобы удержать колесо за шаг, поэтому 20-кратный повтор работает как грубый
стабилизатор. Это объясняет и §139.10 (60/30 и 100/50 сделали хуже — это 60- и 100-кратное
приложение), но означает, что схема не является согласованной по силе ни при каком числе итераций.

### §140.8a. Причинная проверка: залипание монотонно по числу итераций

§139.10 гнал число итераций ВВЕРХ (20/10 → 60/30 → 100/50) и всякий раз становилось хуже; тогда это
прочли как «замок держится точнее». Недостающая половина свипа — вниз. `jolt_wm4_drive_vel_steps`,
чистый ini, n=3, чередование арм, плюс отдельный прогон с чтением РЕАЛЬНОГО числа итераций из
`solveCalls/warmStartCalls` собственного лога каждого арма:

| задано | фактически итераций | `jolt_travel_m` | застряло <2 м |
|---|---|---|---|
| 1 / 4 / 8 | **10** (см. ниже) | 6.82, 6.94, 7.76 / 7.25, 7.13, 6.92 / 7.18, 7.14, 6.53 | **0/9** |
| 12 | 12.00 | 5.00 | 0/1 |
| 16 | 16.00 | 1.16 | 1/1 |
| 20 (дефолт) | 20.00 | 0.797, 0.812, 0.756 | **3/3** |

Зависимость монотонная: 10 итераций — 7 м, 12 — 5 м, 16 — 1.2 м, 20 — 0.8 м, а дальше по §139.10
ещё хуже. Это ровно та подпись, которую даёт переприкладывание: суммарный доставленный импульс
трения пропорционален числу итераций, и выше ~14 он подавляет машину.

**Плато на 1/4/8 — не эффект, а артефакт, и он проверен, а не предположен.** Jolt берёт МАКСИМУМ
переопределений по островy, а 0 значит «взять умолчание», поэтому переопределение НИЖЕ умолчания
ничего не снижает. В логе арма с заданным 4 счётчики дают `solveCalls/warmStartCalls = 6590/659 =
10.00` — то есть все три нижних арма на самом деле шли на десяти итерациях, отсюда и одинаковый
результат. (Скрипт §140.8 сначала вернул на этих армах `?`: последним в срезе оказалась строка
свежепересобранного констрейнта с `warmStartCalls=0`, и разность вышла нулевой. Починено чтением
напрямую, а не выброшено как «шум».)

**Понижение итераций — это НЕ фикс, а подтверждение диагноза.** Даже на лучшем арме тень едет 7.1 м
против эталонных 13.2 (54%), на 12 итерациях — 5.0 против 11.1 (45%). Схема не согласована по силе
ни при каком числе итераций; менять умолчание `wm4_drive_vel_steps` ради этого нельзя — это
подкрутка симптома ([[feedback_no_runtime_patch_hacks]]), к тому же режущая точность решателя всем
остальным констрейнтам в островe.

**Настоящий фикс** — сделать трение НАСТОЯЩИМ констрейнтом: накопленный `lambda`, ограниченный
силовой способностью шины (`D` из Pacejka), приращение за итерацию, стремящееся к нулю по мере
сходимости, — ровно как `ContactConstraintManager` в самом Jolt делает касательные оси через
`AxisConstraintPart`. Тогда доставленный импульс перестаёт зависеть от числа итераций, а модель
шины задаёт ПРЕДЕЛ, а не то, что складывается двадцать раз подряд. Это задача #67.

## §140.9. Задача #67: трение как настоящий констрейнт — половина результата

Реализовано под флагом `[jolt_harness] wm4_friction_constraint` (умолчание 0). На каждый активный
слот две касательные оси `AxisConstraintPart` — катящаяся `t = normalize(axle x n)` и боковая
`l = n x t` — со своим накопленным `lambda`, тёплым стартом наравне с нормальной осью и
ограничением по кругу трения `mu*lambda_n`. Приращение за итерацию теперь только ПОПРАВКА и
затухает по мере сходимости.

Два места, где `r2 = -R*n` используется намеренно по ПРОТИВОПОЛОЖНЫМ причинам, стоит держать в
голове при правках: на нормальной оси точная радиальность гарантирует, что вращение НЕ проецируется
на ось (баг §124.7), на касательных — что проецируется полностью, потому что касательная скорость
в пятне контакта и есть то проскальзывание, ради которого констрейнт существует.

### Гейт 1 — независимость от числа итераций: ПРОЙДЕН

| арм | итераций | `jolt_travel_m` | застряло |
|---|---|---|---|
| `fric=0` | 20 | 0.814, 0.820, 0.822 | **3/3** |
| `fric=0` | 10 | 7.17, 7.43, 7.91 | 0/3 |
| `fric=1` | 20 | 49.9, 43.3, 30.7 | 0/3 |
| `fric=1` | 10 | 39.1, 52.1, 30.9 | 0/3 |

У `fric=0` переход 10 → 20 итераций обрушивает пробег в 9 раз. У `fric=1` средние 40.7 м и 41.3 м —
разница 1.015. Дефект §140.8 устранён. Честная граница утверждения: разброс внутри арма `fric=1`
велик (30.7–52.1), так что доказано «остаточная зависимость тонет в дисперсии», а не «равна нулю»;
против девятикратной базы этого достаточно.

### Гейт 2 — продольная динамика: СОВПАЛА С ЭТАЛОНОМ. Поворот: НЕТ

Скалярный `travel` — это смещение от точки старта, а не длина пути, и большим его делают три разные
причины: едет быстрее, поворачивает шире, не поворачивает вовсе. Реконструкция ОБЕИХ траекторий из
строки divergence (там `jolt com` и `ode com` вместе) разделяет их:

| | длина пути | смещение | прямолинейность | v медиана |
|---|---|---|---|---|
| JOLT `fric=1` | 102.6 м | 81.5 м | **0.79** | 6.15 м/с |
| ODE | 94.0 м | 23.7 м | **0.25** | 6.21 м/с |

Длина пути сходится в пределах 9%, медианная скорость — в пределах 1%. Продольная динамика после
фикса практически correct. Машина просто НЕ ПОВОРАЧИВАЕТ.

Две гипотезы по дороге сюда были опровергнуты измерением, обе мои:
1. **Сцепление.** Свип `grip` дал ОБРАТНЫЙ знак: 2.0 → ratio 3.11, 1.0 → 3.85, 0.5 → 4.68. Меньше
   сцепления — больше смещение, что и подсказало, что дело в ширине дуги, а не в скорости.
2. **Артефакт стенда с углом руля** (§140.2g: сценарный `steer=1.0` — это 1.0 радиан, тень режет по
   `pi/4`, ODE нет). Повтор с `steer=0.785` для обоих движков: ratio 3.44 против 3.11. Не сдвинулось.
   Артефакт реален и по-прежнему открыт, но НЕ он причина.

## §140.9a. Дефицит боковой силы ПОД ТЯГОЙ — воспроизводится, механизм НЕ найден

Прямой поведенческий тест без всякой новой диагностики: разогнаться прямо, затем повернуть в упор —
один раз под газом, один раз накатом.

| | прямолинейность JOLT | прямолинейность ODE |
|---|---|---|
| под газом | 0.85, 0.83 | 0.57, 0.62 |
| накатом | **0.48, 0.56** | 0.58, 0.55 |

Накатом тень поворачивает КАК ЭТАЛОН. Под газом — идёт прямо. Дефицит строго связан с продольной
нагрузкой.

**Моя гипотеза о причине и её опровержение.** Первая версия `SolveFrictionConstraintForSlot` делила
круг ПОСЛЕДОВАТЕЛЬНО: катящаяся ось берёт весь бюджет, боковой достаётся `sqrt(budget^2 - l0^2)`.
Под газом продольный спрос забирает всё — боковой ноль — поворачивать нечем. Я так и написал в
комментарии («известная асимметрия»), недооценив её. Замена на симметричное деление (оба порога
считаются от ПРЕДЫДУЩЕЙ итерации, каждый с полом в 30% бюджета, чтобы насыщенный сосед не мог
обнулить другого) — и результат: 0.95, 0.84 против прежних 0.85, 0.83. **Не помогло.** Значит
голодание по порогу — не механизм, и гипотеза закрыта измерением, а не оставлена «вероятной».

Симметричное деление оставлено в коде: последовательное имело доказанное голодание, это — нет,
и его перерасход круга ограничен 4.4% против 41% у простого квадратного порога.

**Что осталось непроверенным** (в порядке дешевизны): (а) перекрёстная связь через `r2` — обе оси
бьют по телу колеса с малым моментом инерции, продольный импульс сильно меняет вращение, а боковая
ось видит уже изменённую скорость точки контакта; (б) предел момента привода: под стик-констрейнтом
колесо не может проскользнуть, и `Velocity`-мотор давит, пока трение не насытится — под штрафной
моделью это ограничивал дребезг; (в) `mu*lambda_n` как бюджет против `D` из §125 — это РАЗНЫЕ
величины (одна от импульса констрейнта, другая от пружины пересчёта), и их расхождение не измерено.

**Умолчание остаётся 0.** Гейты 3–5 (регрессии `Molokovoz01`/`Ural01`, флотский свип, живая
проверка) не запускались и не должны, пока поворот под тягой не починен.

## §140.10. Задача #68: боковой констрейнт не отказывает — он СЛИШКОМ хорошо работает

Прямой замер проскальзывания в фазе поворота (`wm4_friction_constraint=1`, агрегирование ПО КАДРАМ,
затем медиана — не по столбцам, см. предупреждение в конце):

| | \|v_lat\| в пятне контакта | поворачивает? |
|---|---|---|
| под газом | **0.017 м/с** | нет, прямолинейность 0.85 |
| накатом | 0.203 м/с | да, как эталон |

Боковой констрейнт под газом не проваливается. Он выполняется ПОЧТИ ИДЕАЛЬНО — и именно поэтому
машина не поворачивает.

### Кинематическое доказательство

Система координат корпуса: `x` — вправо, `z` — вперёд, `omega` — рыскание. Скорость точки на
продольной станции `s` равна `(vx + omega*s, vz)`. У колеса, повёрнутого на `delta`, направление оси
(боковое) есть `(cos d, -sin d)`. Условие нулевого бокового проскальзывания:

```
(vx + omega*s)*cos d - vz*sin d = 0      <=>      vx + omega*s = vz*tan d
```

У `Mirotvorec01` передняя ось `s=+3.6` и СРЕДНЯЯ ось `s=0.0` повёрнуты на ОДИН И ТОТ ЖЕ угол.
Обе строки правой частью имеют `vz*tan d`, отличаются только коэффициентом при `omega`. Вычитание:

```
omega*(s1 - s2)*cos d = 0     ->     omega = 0
```

Дальше нерулевая задняя ось даёт `vx = 0`, а рулевая строка — `vz = 0`. Определитель системы
`det(A) = 1.8 != 0`, то есть ЕДИНСТВЕННОЕ точное решение — полная неподвижность.

**Вывод: для машины с двумя рулевыми осями на РАЗНЫХ продольных станциях, повёрнутыми на ОДИН угол,
жёсткое «ноль бокового проскальзывания» несовместимо с каким бы то ни было движением.** Солвер
сходится к компромиссу: крошечная остаточная невязка (0.017 м/с) и `omega ~ 0` — то есть едет прямо.
Ровно то, что измерено.

Это же объясняет, почему у обычной машины с ОДНОЙ рулевой осью проблемы нет: там две строки на три
неизвестных, остаётся однопараметрическое семейство движений, и поворот возможен. Затронуты только
машины с двумя и более рулевыми осями — `Mirotvorec01`, `Cruiser01` (8к/4р), `Tank01` (6к/4р). И это
не «список машин-исключений»: причина общая и сформулирована через геометрию, а не через имена.

### Что это значит для задачи #67

Настоящая шина ОБЯЗАНА проскальзывать вбок, чтобы такая машина поворачивала, и боковая сила должна
расти с углом увода, а не запрещать увод. Именно это давала штрафная модель Pacejka и именно это
даёт эталонный ODE своими мягкими CFM/ERP-контактами. Констрейнт с нулевой целевой скоростью такой
свободы не оставляет.

Направление фикса, для отдельной задачи: сохранить накопленный `lambda` (он вылечил зависимость от
числа итераций, §140.9 гейт 1), но перестать целиться в нулевую скорость. Либо ограничивать `lambda`
силой Pacejka при ТЕКУЩЕМ угле увода (тогда кривая скольжения возвращается, а сходимость остаётся),
либо дать оси податливость (CFM-подобную мягкость), чтобы остаточный увод был разрешён.

Не объяснено и оставлено честно: почему накатом невязка ВЫШЕ (0.203 против 0.017), хотя бюджет тот
же. Ожидание было обратным. На вывод это не влияет — связь «нулевой увод <=> нет рыскания» доказана
алгебраически, а не выведена из этой асимметрии.

### Опровергнуто в этой задаче

| Гипотеза | Как проверялась | Результат |
|---|---|---|
| Перекрёстная связь через `r2` | аналитически: `l` = ось, вращение даёт вклад только вдоль `t` | невозможно по построению |
| Голодание боковой оси по бюджету | свип `wm4_fric_floor` 0.3 / 0.6 / 1.0 | прямолинейность 0.89 → 0.93 → 0.93, плоско; при 1.0 боковой бюджет вдвое выше эталонного и всё равно не поворачивает |

Замечание к первому отказу этой гипотезы (§140.9a): я закрыл её по одному значению 0.3, и это было
преждевременно — при `grip=2.0` порог 0.3 даёт эффективное боковое `mu` 0.6, ниже эталонных ~1.0,
то есть арм стоял по ту же сторону от эталона и различить гипотезу не мог. Теперь она закрыта
измерением, которое действительно различало.

Побочно: контрольный НАКАТНЫЙ поворот при росте порога ПОРТИТСЯ (0.52 → 0.74 → 0.75 против эталонных
~0.53–0.67) — перерасход круга у квадратного порога. Ещё одна причина держать `wm4_fric_floor`
низким; умолчание 0.3 не менялось.

> **Метод.** Медианы по СТОЛБЦАМ на этих дребезжащих поканальных данных дали в §140.8/§140.9 три
> разных ложных чтения подряд (столбцы, чьи медианы взяты из разных кадров и не сходятся друг с
> другом). Сначала агрегировать внутри кадра, потом брать медиану по кадрам.

## §140.11. Задача #69: режим 2 — податливое трение. Гейты 1 и 3 пройдены, гейт 2 — нет

`wm4_friction_constraint` стал трёхпозиционным: 0 — старая штрафная модель, 1 — стик-констрейнт
(§140.9), 2 — податливый. Режим 2 сохраняет накопленный `lambda`, но потолок каждой оси — это сила
Pacejka при проскальзывании НА НАЧАЛО ШАГА, прогнанная через тот же круг трения, что применяет
`wheelmodel_core`. Ниже потолка шина держит (правильное трение покоя), на потолке — скользит, и
боковая сила растёт с углом увода, а не запрещает увод. Именно это и требовалось по §140.10.

Потолок считается ОДИН раз за шаг сознательно: констрейнт весь шаг гасит то самое проскальзывание,
из которого потолок выведен, так что пересчёт на каждой итерации замкнул бы петлю (увод → 0 тянет
потолок → 0, это отпускает колесо, увод отрастает). Определения проскальзывания взяты у
`wheelmodel_core` (та же `vpAt`, та же пружина `F_n`), а не написаны заново, чтобы пути не разошлись.

### Сначала — исправление метода, без которого выводы были бы ложными

Метрика `straightness = смещение/путь` оказалась слишком шумной. У режима 1 она давала 0.85/0.83,
потом 0.93/0.86, потом 0.90/0.95 в трёх разных сессиях при ОДНОМ конфиге; у режима 2 — 0.73/0.69,
а на следующем прогоне 0.96/0.94. При n=2 эти выборки неразличимы, и мой вывод «режим 2 улучшает
поворот», сделанный по двум прогонам, опирался на шум. В хендоффе уже записано, что для рулевых
замеров нужно n>=3 — я это правило проигнорировал.

Заменено на НАКОПЛЕННОЕ ИЗМЕНЕНИЕ КУРСА за фазу поворота (по направлению движения между соседними
отсчётами COM): оно измеряет ровно то, о чём вопрос, не зависит от скорости, и берётся отношением
jolt/ode ПОКАДРОВО, так что разброс эталона делится, а не складывается. Эталон при этом оказался
стабильным: 175–201° во всех восьми прогонах — значит шумела метрика, а не симуляция.

### Гейт 1 — поворот под тягой: ПРОЙДЕН (n=4, чередование)

| арм | поворот jolt/ode (1.0 = совпало) | повернул, град |
|---|---|---|
| режим 1 (стик) | **0.58** (разброс 0.45–0.64) | 85, 121, 123, 103 |
| режим 2 (податливый) | **0.96** (разброс 0.67–1.27) | 196, 238, 117, 171 |

Диапазоны не пересекаются (максимум режима 1 — 0.64, минимум режима 2 — 0.67).

### Гейт 3 — репро залипания §140: ПРОЙДЕН, и не просто «поехала»

Тот же сценарий, что во всех прежних таблицах, 20 итераций (штатное значение):

| прогон | jolt, м | ode, м | отношение |
|---|---|---|---|
| 1 | 14.06 | 14.13 | 0.99 |
| 2 | 11.21 | 13.65 | 0.82 |
| 3 | 12.07 | 11.01 | 1.10 |

Среднее 0.97, застряло 0/3. Против 0.75/0.75/0.78 и 3/3 у режима 0. То есть пробег теперь не просто
ненулевой, а совпадает с эталоном.

### Гейт 2 — независимость от числа итераций: НЕ ПРОЙДЕН

| режим | 10 итераций | 20 итераций | изменение |
|---|---|---|---|
| 0 (штрафной) | 7.35 м | 0.76 м | **9.7x**, и 20 — ХУДШИЙ конец |
| 2 (податливый) | 27.04 м | 12.45 м | **2.2x**, и 20 — ЛУЧШИЙ конец |

Гейт формулировался как «должно стать плоско». Плоско не стало. Но характер зависимости сменился на
противоположный: у режима 0 рост итераций УХОДИЛ от правильного ответа (расходящееся
переприкладывание), у режима 2 — ПРИБЛИЖАЕТ к нему (недосходимость при малом бюджете; на штатных 20
итерациях результат совпадает с эталоном). Это улучшение, но не то, что обещал гейт, и записывается
как невыполненный, а не как «почти».

### Состояние задачи #69

Пройдено: гейт 1 (поворот), гейт 3 (репро). Не пройдено: гейт 2 (остаточная зависимость 2.2x).
Не запускалось: регрессии `Molokovoz01` (§138) и `Ural01` (§124.11), флотский свип 23 машин, живая
проверка за рулём. **Умолчание остаётся 0.**

## §140.12. Гейты 4 и 5: регрессии чисты, флотский вывод НЕ получен

### Гейт 4 — регрессии: ПРОЙДЕН

Два отказа, которые этот путь уже вызывал по разу каждый. Удержание на ручнике 12 с, n=2, apply=0:

| арм | дрейф тени, м | max \|wheelAngVel\|, рад/с |
|---|---|---|
| `Molokovoz01` режим 0 | 0.003, 0.003 | 0.00, 0.00 |
| `Molokovoz01` **режим 2** | 0.000, 0.000 | 0.00, 0.00 |
| `Ural01` режим 0 | 0.000, 0.000 | 0.00, 0.00 |
| `Ural01` **режим 2** | 0.000, 0.000 | 0.00, 0.00 |

Разгон `omega` из §138 не вернулся. Оговорка: известный дефект `Ural01` — это ~40% отказов на
ПЕРЕКЛЮЧЕНИИ машин, а здесь каждый прогон пинит его напрямую свежим запуском, так что n=2
показывает «режим 2 не добавляет нового», но ничего не говорит про ту старую частоту.

### Гейт 5 — флотский свип 23 машин: НЕ ПРОЙДЕН, и в рулевой части НЕИЗМЕРИМ

| сценарий | среднее \|ratio−1\| | медиана | улучшилось | разброс |
|---|---|---|---|---|
| `accel_brake` | 0.21 → 0.21 | 0.18 → 0.21 | 6/22 | 0.65–1.54 → 0.65–**1.23** |
| `accel_steer_brake` | 0.75 → 0.86 | 0.61 → 0.53 | 14/22 | 0.14–4.70 → 0.09–5.98 |

По прямой чистого выигрыша нет: верхние выбросы подтянулись (`Dozer01` 1.52→1.20, `Traktor01`
1.54→1.23, верх разброса 1.54→1.23), но большинство машин чуть отошло от 1.0.

Рулевую колонку читать НЕЛЬЗЯ: это n=1 на машину по метрике travel-ratio, той самой, у которой
§140.11 намерил четырёхкратный разброс при неизменном конфиге. Отсюда и 0.09–5.98. Цитировать из
неё «улучшилось 14/22» или «медиана 0.61→0.53» — это ровно та ошибка, которую я уже допустил
однажды, только теперь во флотском масштабе. Скрипт свипа написан до того, как это стало известно.

### Прицельный замер вместо свипа — и он тоже не дал ответа

Пять машин, разбитых по ГЕОМЕТРИЧЕСКОМУ критерию §140.10, n=3, метрика — накопленное изменение
курса (та, что в §140.11 разделила армы чисто).

| машина | класс | режим 0 | режим 2 | эталон стабилен? |
|---|---|---|---|---|
| `Mirotvorec01` | 2 рулевых оси | **0.07** (0.06, 0.11, 0.04) | **1.20** (1.78, 1.19, 0.62) | ДА (CV 0.09) |
| `Bug01` | 1 рулевая ось | 1.27 (1.30, 1.25, 1.26) | **1.15** (1.14, 1.11, 1.20) | ДА (CV 0.06) |
| `Cruiser01` | 2 рулевых оси | 1.61 | 5.18 | нет (CV 0.29) |
| `Tank01` | 2 рулевых оси | 0.89 | 0.51 | нет (CV 0.28) |
| `Scout01` | 1 рулевая ось | 2.49 | 12.46 | нет (CV 0.49) |

**Предсказание («затронутые заметно улучшатся, контроли почти не сдвинутся») не подтвердилось и не
опровергнуто** — из пяти машин пригодны для чтения только две, и обе улучшились: `Mirotvorec01`
кардинально (0.07 → 1.20, то есть от «вообще не поворачивает» к околопаритету), `Bug01` слегка и
устойчиво (1.27 → 1.15, разброс сузился с 1.25–1.30 до 1.11–1.20).

У трёх остальных сам ЭТАЛОН в этом сценарии поворачивает нестабильно (CV 0.28–0.49) и на малые
углы, так что знаменатель отношения шумит сильнее числителя и отношение не значит ничего. Это
методическое ограничение, а не результат: их нельзя записывать ни в улучшение, ни в регрессию.

**Что нужно, чтобы гейт 5 закрылся**: сценарий, дающий устойчивый опорный поворот для машин с малым
собственным рысканием (меньший угол руля, более длинная установившаяся дуга), либо переход на
АБСОЛЮТНУЮ метрику (скорость рыскания против эталонной) вместо отношения. До этого вывод
«общий фикс или одна топология» остаётся открытым.

### Итог по задаче #69

| гейт | статус |
|---|---|
| 1. Поворот под тягой | ✅ 0.96 против 0.58 (n=4, диапазоны не пересекаются) |
| 2. Независимость от итераций | ❌ остаточные 2.2x (было 9.7x), задача #70 |
| 3. Репро залипания §140 | ✅ 0/3, пробег совпал с эталоном (0.97) |
| 4. Регрессии `Molokovoz01`/`Ural01` | ✅ чисто |
| 5. Флотский свип | ⚠️ не получен: по прямой нет выигрыша, рулевая часть неизмерима |
| 6. Живая проверка за рулём | не запускалась |

**Умолчание остаётся 0.** Менять его сейчас нельзя: гейт 5 не закрыт, а по прямой на флоте
измерено лёгкое ухудшение медианы (0.18 → 0.21).

## §140.13. Задача #71: установившаяся окружность — затронутые машины улучшаются, но гейт 5 всё ещё открыт

Чтобы убрать артефакт полного упора руля и переходный процесс, повторный замер использовал
`steer=0.35` рад (20°, ниже предела `π/4`), газ 0.5 после разгона и последние 4 секунды
9-секундной дуги. Рыскание считалось по направлению движения между отсчётами COM; на каждую
машину и режим — n=3, прогоны чередовались. Эталонный `mode 0` одновременно служил проверкой
стабильности знаменателя: CV эталонной скорости рыскания должен быть <0.15.

| машина | класс | CV эталона | режим 0, jolt/ODE | режим 2, jolt/ODE | абсолютная ошибка 0 → 2, °/с |
|---|---|---:|---:|---:|---:|
| `Mirotvorec01` | затронутая | 0.03 | 0.42 (0.49, 0.45, 0.33) | **0.65** (0.77, 0.39, 0.80) | **18.8 → 11.8** |
| `Cruiser01` | затронутая | 0.06 | 0.52 (0.39, 0.59, 0.58) | **0.80** (0.55, 1.08, 0.76) | **24.9 → 10.9** |
| `Tank01` | затронутая | 0.17 | 0.55 (1.12, 0.37, 0.17) | 0.87 (1.17, 0.84, 0.59) | **13.7 → 4.1** |
| `Bug01` | контроль | 0.42 | 1.60 | 2.42 | 7.5 → 25.1 |
| `Scout01` | контроль | 0.38 | 2.40 | 1.37 | 22.9 → 6.6 |

Итог: у двух из пяти машин (`Mirotvorec01`, `Cruiser01`) эталон устойчив, и обе показывают
улучшение режима 2. `Tank01` находится рядом с порогом, но CV=0.17 — честно исключён из
отношения. У контролей эталон непригоден (CV 0.38–0.42); абсолютная ошибка у них ведёт себя
разнонаправленно. Это согласуется с гипотезой о пользе фикса для затронутой геометрии, но не
является флотским доказательством: пригодных машин всего две, а `Bug01` и `Scout01` нельзя
сравнивать через ratio.

Задача #71 закрыта как **диагностическая попытка**, гейт 5 — **не закрыт**. Следующий осмысленный
шаг — не усреднять непригодные отношения, а либо подобрать для `Tank01` и контролей устойчивую
дугу (меньший угол и более длинное установившееся окно), либо заранее принять абсолютную
метрику `|jolt−ODE|` и повторить её на флоте с n≥3. Умолчание остаётся 0.

## §140.14. Задача #70: отношение lambda к потолку — простая гипотеза недосходимости не объяснила 1.6x

Добавлен только диагностический вывод `fricCapRatio=par/lat` в строку `docs §140`: это максимум
по активным слотам `|lambda| / mMaxFricLambda` за завершившийся шаг, отдельно для продольной и
боковой оси. Физика не менялась. Значение снимается перед подготовкой следующего шага, чтобы
печатался действительно финальный lambda, а не текущий потолок после перестроения контакта.

Чистый прогон `Mirotvorec01`, режим 2, `apply=0`, n=3, с полной перезагрузкой между прогонами:

| итерации | jolt, м | ODE, м | par ratio | lat ratio |
|---:|---:|---:|---:|---:|
| 10 | 13.460 | 15.038 | 0.573 | 1.000 |
| 10 | 8.450 | 16.983 | 0.703 | 0.706 |
| 10 | 10.075 | 15.888 | 0.625 | 1.000 |
| 20 | 17.682 | 16.875 | 0.135 | 1.000 |
| 20 | 16.040 | 15.274 | 0.177 | 1.000 |
| 20 | 17.741 | 13.900 | 0.239 | 1.000 |

Средний travel: **10.662 м → 17.154 м**, то есть зависимость 1.61x; CV внутри групп — 0.20
и 0.05. Боковая lambda уже достигает потолка на 10 итерациях в 2/3 прогонов и на 20 — во
всех трёх; продольная остаётся ниже потолка (0.63 против 0.18). Поэтому гипотеза «при 10
итерациях lambda недобирает потолок, а при 20 до него доходит» не объясняет наблюдение: для
боковой оси потолок уже достигнут, а travel всё ещё меняется. Это сужает поиск к связи
между осями/порядком PGS и к тому, что меняется сама траектория и, следовательно, потолок,
но не оправдывает пока новую физическую правку.

Задача #70 остаётся **не закрыта**. Диагностический канал оставлен под флагом режима 2 и не
влияет на режим 0; следующее измерение должно разделить межосевую связь (Jacobi-параметризацию)
и изменение входного slip/потолка по кадрам, прежде чем трогать solver.

## §140.15. Задача #70: порядок осей — текущий порядок подтверждён, обратный хуже

Покадровая строка `docs §140` расширена полями `fricLambda`, `fricCap` и `fricSlip` по обеим
осям. Для контролируемого A/B добавлен `[jolt_harness] wm4_fric_axis_order`: `0` — текущий
порядок rolling/longitudinal → lateral, `1` — обратный. Значение по умолчанию осталось 0.

Матрица `Mirotvorec01`, режим 2, `apply=0`, полная перезагрузка, n=3 на каждую ячейку:

| порядок | 10 итераций, jolt м | 20 итераций, jolt м | среднее 10 → 20 | застреваний |
|---|---:|---:|---:|---:|
| `0: par → lat` | 14.29, 13.40, 11.23 | 14.18, 13.60, 13.21 | **12.972 → 13.661 (1.05x)** | 0/3, 0/3 |
| `1: lat → par` | 12.17, 8.93, 6.48 | 13.70, 12.31, 13.86 | **9.195 → 13.289 (1.45x)** | 0/3, 0/3 |

Текущий порядок лучше; обратный порядок не вызывает нового залипания, но заметно усиливает
зависимость от бюджета solver’а. Поэтому менять порядок не нужно: штатный `par → lat` остаётся
контролем и дефолтом.

### Повторная проверка регрессий

`Molokovoz01` и `Ural01`, удержание на ручнике 12 с, режимы 0/2, n=2:

| машина | режим 0 | режим 2 |
|---|---|---|
| `Molokovoz01` | drift 0.000 м, max wheelAngVel 0.00 рад/с (2/2) | drift 0.000 м, max wheelAngVel 0.00 рад/с (2/2) |
| `Ural01` | drift 0.000 м, max wheelAngVel 0.00 рад/с (2/2) | drift 0.000 м, max wheelAngVel 0.00 рад/с (2/2) |

**Статус задачи #70:** повторная чистая матрица проходит gate 2 (`1.05x` для штатного порядка),
gate 3 остаётся пройден (`0/3`), регрессии чисты. Исторические 2.2x из §140.11 сохранены как
предыдущий неудачный замер. Новая диагностика не меняет дефолт и не требует включать режим 2.
## §140.16. Артефакт стенда: нормализация угла руления

В `source/fix/testharness.cpp` входной `steer` теперь ограничивается диапазоном `[-π/4, +π/4]` ещё при разборе сценария. Это тот же предел, который использует игровая модель колеса. Поэтому и `Vehicle::m_steerRadians`, и прямое обновление `Wheel::m_curAngle` получают одинаковое физически достижимое значение; стенд больше не создаёт состояние `1.0` рад (57°), которого игровая модель не может произвести.

Изменение ограничено амплитудой входа: ограничитель скорости поворота и физические дефолты не менялись. Release-сборка прошла, DLL развернута.

Повторный steady-turn прогон (`steer=0.35` рад, газ 0.5, n=3) дал стабильный ODE-эталон для `Mirotvorec01` (CV 0.08), `Cruiser01` (0.08) и `Tank01` (0.14); `Bug01` и `Scout01` остаются шумными (0.28 и 0.27). Средняя абсолютная ошибка `|jolt−ODE|`, °/с:

| машина | режим 0 | режим 2 |
|---|---:|---:|
| `Mirotvorec01` | 19.079 | 12.022 |
| `Cruiser01` | 21.275 | 22.552 |
| `Tank01` | 6.970 | 9.930 |
| `Bug01` | 16.458 | 10.590 |
| `Scout01` | 15.867 | 7.630 |

Это подтверждает устранение рассинхрона входного угла, но не закрывает gate 5: для `Bug01`/`Scout01` эталон всё ещё нестабилен, а по трём пригодным машинам эффект режима 2 разнонаправлен. Следующий шаг — повторить fleet-сравнение по абсолютной метрике с тем же нормализованным сценарием; режим 2 и его дефолт пока не включать.
## §140.17. Fleet steady-turn: абсолютная метрика после исправления стенда

Добавлен воспроизводимый runner `tools/testharness/fleet_steady_turn_modes.py`. Он запускает все 23 машины отдельно для `wm4_friction_constraint=0` и `2`, использует steady-turn (`steer=0.35` рад, газ 0.5) и сравнивает медианную yaw-rate в установившемся окне по MAE `|jolt−ODE|`. Прогон завершён без падений: 46/46 запусков; `Ural01` исключён из сравнения, поскольку Jolt yaw-rate не сформировалась.

| показатель | результат |
|---|---:|
| сравнимых машин | 22 |
| mode 2 лучше | 12 |
| mode 2 хуже | 10 |
| средний MAE mode 0 → mode 2 | **17.275 → 14.686 °/с** |
| медианный MAE mode 0 → mode 2 | **15.148 → 11.441 °/с** |

Наиболее заметные улучшения: `Mirotvorec01` 26.187→1.498, `Sml301` 52.559→4.973, `Hunter01` 30.379→10.046, `Fighter01` 22.872→7.513 °/с. Наиболее заметные ухудшения: `Bug01` 10.517→34.412, `Traktor01` 15.853→37.188, `Belaz01` 3.256→20.867, `Cruiser01` 15.148→26.181 °/с.

Это полезный fleet-сигнал, но не доказательство для смены дефолта: `n=1` не позволяет оценить CV ODE-эталона, а steady-turn с `steer=0.35` намеренно не пересекает предел `π/4` и потому не является отдельной проверкой clipping для входа `steer=1.0`. Gate 5 остаётся открытым. Следующий измерительный шаг — `n=3` для репрезентативных улучшившихся и ухудшившихся групп (`Mirotvorec01`, `Sml301`, `Hunter01`, `Fighter01` против `Bug01`, `Traktor01`, `Belaz01`, `Cruiser01`) с тем же сценарием.
## §140.18. Исправление метрики yaw: Ural возвращён в fleet

§140.17 использовал yaw-rate, восстановленную из направления движения COM. Для `Ural01` Jolt COM практически не двигался, поэтому направление было неопределено и машина ошибочно выпала из таблицы. Это была проблема измерительного инструмента, а не основание исключать машину.

В `DivergenceSample` добавлены native angular velocities: абсолютная компонента world-up (`Y`) Jolt и ODE логируется как `yaw=(jolt/ode)deg/s`. Runner теперь берёт медиану этих значений в том же установившемся окне. Release-сборка прошла, DLL развернута; отдельная проверка Ural дала mode 0 `55.058°/с`, mode 2 `42.289°/с`.

Полный повтор fleet (`n=1`, 23 машины × 2 режима, 46/46) дал:

| показатель | результат |
|---|---:|
| сравнимых машин | **23/23** |
| mode 2 лучше | 12 |
| mode 2 хуже | 11 |
| средний MAE mode 0 → mode 2 | **22.100 → 19.761 °/с** |
| медианный MAE mode 0 → mode 2 | **15.233 → 20.849 °/с** |
| Ural01 mode 0 → mode 2 | **54.411 → 54.630 °/с** |

Сильные улучшения mode 2: `Scout03` 46.155→20.849, `ArcadeScout01` 21.924→3.144, `Bug01` 34.736→21.736, `Belaz01` 37.704→25.121 °/с. Сильные ухудшения: `Dozer01` 1.669→23.998, `Sml301` 8.597→26.595, `Scout01` 13.621→28.344 °/с.

Среднее улучшилось за счёт нескольких крупных выигрышей, но медиана ухудшилась; `n=1` также не даёт CV эталона. Поэтому gate 5 по-прежнему не закрыт, а `wm4_friction_constraint` по умолчанию остаётся `0`. Результат сохранён в `fleet_steady_turn_modes_v2.json`; §140.17 следует считать предварительной таблицей с устаревшей path-derived метрикой.
## §140.19. Representative n=3 с Ural и native-yaw метрикой

После исправления §140.18 выполнен representative-прогон `n=3`: 8 машин × 2 режима × 3 повтора = **48/48**, без пропусков. Угловая ошибка — MAE `|jolt yaw − ODE yaw|` в установившемся окне; Ural измеряется тем же native angular-velocity каналом, а не исключается.

| машина | mode 0 MAE | mode 2 MAE | Δ mode2−mode0 | ODE CV mode0 / mode2 |
|---|---:|---:|---:|---:|
| `Mirotvorec01` | 11.192 | **6.737** | -4.455 | 0.442 / **0.137** |
| `Bug01` | **7.942** | 18.933 | +10.991 | 0.272 / 0.285 |
| `Cruiser01` | 22.633 | **7.276** | -15.357 | **0.038** / 0.207 |
| `Dozer01` | **7.181** | 11.132 | +3.951 | 0.362 / 0.267 |
| `Scout01` | **22.852** | 23.023 | +0.171 | 0.313 / 0.468 |
| `Scout03` | 52.173 | **30.995** | -21.177 | 0.509 / 0.404 |
| `Sml301` | **2.423** | 21.343 | +18.920 | **0.052** / 0.354 |
| `Ural01` | 52.950 | **52.207** | -0.743 | **0.035** / **0.046** |

Результат не даёт общего разрешения на mode 2: выигрыши и проигрыши разнонаправлены, а у большинства машин ODE CV выше порога 0.15 хотя бы в одном режиме. Уral при этом валиден как измерительный datapoint и показывает почти нейтральный эффект (`52.950 → 52.207`). Gate 5 остаётся открытым, дефолт `wm4_friction_constraint=0`; данные сохранены в `representative_n3.json`.
