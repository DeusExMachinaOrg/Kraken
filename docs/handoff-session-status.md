# Handoff: статус Jolt-интеграции на 2026-07-22 (после §18-21: профилирование, фикс, ретюнинг, Nelder-Mead)

Снимок состояния для передачи работы. Цепочка сессии: Stages -1..3 → §8.5 тюнинг → §17 профилирование
→ §18 раздельное измерение → §19 фикс redundant-StepPhysics-бага → §20 профилирование рендера +
переизмерение тюнинга → **§21 Nelder-Mead взамен coordinate descent**. Читать
`docs/jolt-integration-techanalysis.md` §18-21, не эту сводку, за деталями/кодом/цифрами.

## Что сделано в этой сессии (по порядку)

1. **Инструментация + находка + фикс** (§18-19): `PhysicsSystem::Update()` вызывался до 17 раз за
   кадр вместо одного, исправлено. Jolt-оверхед свёлся к статистическому шуму относительно baseline.
2. **Профилирование рендера** (§20.1, PerfView, с разрешения пользователя на скачивание) — реальное
   узкое место для FPS в этой сцене - рендер/тени (40%+ кадра), не физика. По решению пользователя
   не продолжено в этой сессии, но задокументировано и готово к работе при отдельном запросе.
3. **Переизмерение тюнинга на исправленном коде** (§20.2) — coordinate descent нашёл новый оптимум
   (score 29.53→5.08, улучшение 82.8%, против ~2% на баговом коде).
4. **Nelder-Mead взамен coordinate descent** (§21, по прямой просьбе пользователя) — полностью
   заменил старый алгоритм (не оставлен как fallback). Реализовано и дважды проверено (implement
   agent собрал сам, verify agent пересобрал независимо). Живой прогон дал сопоставимый, но не
   строго лучший результат на этом единичном сравнении (5.22 vs coordinate descent's 5.08) - два
   алгоритма нашли РАЗНЫЕ комбинации параметров с близким качеством, похоже на несколько
   сопоставимых локальных оптимумов, а не один острый глобальный. **Coordinate descent'а больше нет
   в коде** - autotune теперь всегда Nelder-Mead.
5. Всё провалидировано воркфлоу (implement→adversarial verify), везде вердикт `safe_to_build`.
   **Ничего не закоммичено** — `source/fix/joltshadow.cpp` + оба .md файла поверх коммита `7722c61`
   на ветке `jolt`.

## Текущее состояние деплоя (`F:\HTA_Kraken`)

- `hta.exe` **не запущен**.
- `kraken.dll` — актуальная сборка ветки `jolt`, включая фикс §19 И Nelder-Mead §21.
- `data/kraken.ini` — **`[jolt] enabled=0`** (исходная настройка машины). В конце файла — лучшие
  известные тюн-значения от coordinate descent (`susp_frequency=1.80 susp_damping=0.60
  friction_long=0.93 friction_lat=1.00`, score 5.08) - Nelder-Mead-результат этой сессии (5.22) их
  не перезаписал, т.к. маргинально хуже на единственном прямом сравнении. Значения неактивны
  (`enabled=0`), включить — `enabled=1`.
- `data/kraken_testharness/scenario.csv` — переиспользуемый сценарий (один и тот же спавн/манёвр
  использовался и в §20.2, и в §21.2 для прямой сравнимости score между прогонами) - не удалять,
  если нужно продолжать сравнивать методы тюнинга на одном и том же сценарии.
- `C:\Users\etozh\...\scratchpad\PerfView.exe` — скачанный профайлер, для дальнейшего профилирования
  рендера, если понадобится.

## Открытые вопросы / что можно делать дальше

1. **Оптимизация рендера/теней** (§20.1) — не начата, отдельная большая задача (новая область
   реверса - `m3d::SceneGraph`/`QuadPS::Render`/тени), при отдельном запросе.
2. **Более убедительное сравнение coordinate descent vs Nelder-Mead** - текущее сравнение (§21.2) -
   один прогон каждого, разница (5.08 vs 5.22) может быть в пределах шума. Нужны повторы для
   статистически значимого вывода, если это важно.
3. Переизмерить §17 на его исходном окружении - по-прежнему открыто, не приоритет.
4. Stage 4 (§15) по-прежнему не начат.

## Если начинать следующую сессию "с нуля"

Прочитать в таком порядке: этот файл → `docs/jolt-integration-techanalysis.md` §18-21 (самые
свежие разделы) → §12-17 по необходимости. Этот файл можно удалить после того, как начнётся новая
содержательная работа поверх текущего состояния.
